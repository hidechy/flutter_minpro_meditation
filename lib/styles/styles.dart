import 'package:flutter/material.dart';

final dialogBackgroundColor = Colors.blueAccent.withOpacity(0.1);

final touchFeedbackColor = Colors.redAccent.withOpacity(0.2);

const timeSettingDialogTextStyle = TextStyle(
  fontSize: 18,
  color: Colors.white,
);
