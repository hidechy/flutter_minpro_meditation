import 'package:flutter/material.dart';

class PlayButtonPart extends StatelessWidget {
  const PlayButtonPart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'PlayButtonPart',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
