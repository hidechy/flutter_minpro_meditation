import 'package:flutter/material.dart';

class StatusDisplayPart extends StatelessWidget {
  const StatusDisplayPart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'StatusDisplayPart',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
