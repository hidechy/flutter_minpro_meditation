import 'package:flutter/material.dart';

class VolumeSliderPart extends StatelessWidget {
  const VolumeSliderPart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'VolumeSliderPart',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
