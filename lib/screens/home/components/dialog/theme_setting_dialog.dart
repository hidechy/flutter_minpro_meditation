import 'package:flutter/material.dart';

class ThemeSettingDialog extends StatelessWidget {
  const ThemeSettingDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('ThemeSettingDialog');
  }
}
