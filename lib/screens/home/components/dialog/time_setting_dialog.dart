import 'package:flutter/material.dart';

class TimeSettingDialog extends StatelessWidget {
  const TimeSettingDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('TimeSettingDialog');
  }
}
